import { database,ensureSchema } from '../api/_lib/db.js';
import { binghattiAmberhallInventory } from '../api/_lib/binghatti-inventory.js';
await ensureSchema();const sql=database();
for(const x of binghattiAmberhallInventory)await sql`INSERT INTO property_inventory (unit,developer,project,emirate,area,property_type,bedrooms,minimum_price,maximum_price,minimum_size,maximum_size,price_per_sqft,handover,payment_plan_summary,construction_status,suitability,status,source,data_quality,last_updated,is_test) VALUES (${x.unit},${x.developer},${x.project},${x.emirate},${x.area},${x.property_type},${x.bedrooms},${x.minimum_price},${x.maximum_price},${x.minimum_size},${x.maximum_size},${x.price_per_sqft},${x.handover},${x.payment_plan_summary},${x.construction_status},${x.suitability},${x.status},${x.source},${x.data_quality},${x.last_updated},FALSE) ON CONFLICT (unit) DO UPDATE SET developer=EXCLUDED.developer,project=EXCLUDED.project,emirate=EXCLUDED.emirate,area=EXCLUDED.area,property_type=EXCLUDED.property_type,bedrooms=EXCLUDED.bedrooms,minimum_price=EXCLUDED.minimum_price,maximum_price=EXCLUDED.maximum_price,minimum_size=EXCLUDED.minimum_size,maximum_size=EXCLUDED.maximum_size,price_per_sqft=EXCLUDED.price_per_sqft,handover=EXCLUDED.handover,payment_plan_summary=EXCLUDED.payment_plan_summary,construction_status=EXCLUDED.construction_status,suitability=EXCLUDED.suitability,status=EXCLUDED.status,source=EXCLUDED.source,data_quality=EXCLUDED.data_quality,last_updated=EXCLUDED.last_updated,is_test=FALSE,updated_at=NOW()`;
const rows=await sql`SELECT unit,status,data_quality,is_test,price_per_sqft,suitability FROM property_inventory WHERE unit IN ('BAMH-1545','BAMH-634') ORDER BY unit`;if(rows.length!==2||rows.some(x=>x.status!=='active'||x.data_quality!=='verified'||x.is_test))throw new Error('Post-import verification failed.');console.log(JSON.stringify({inserted_or_updated:rows.length,records:rows},null,2));
