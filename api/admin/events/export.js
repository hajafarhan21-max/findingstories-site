import { isAdmin } from '../../_lib/auth.js';import { database } from '../../_lib/db.js';import { method } from '../../_lib/http.js';
const csv=v=>`"${String(v??'').replaceAll('"','""')}"`;
export default async function handler(req,res){if(!method(req,res,['GET']))return;if(!isAdmin(req)){res.statusCode=401;return res.end('Authentication required');}const sql=database();const rows=await sql`SELECT full_name,phone,email,purpose,budget,property_type,preferred_area,purchase_timeline,preferred_event_date,status,assigned_to,lead_score,temperature,source,utm_source,utm_medium,utm_campaign,created_at FROM event_rsvps r JOIN events e ON e.id=r.event_id WHERE e.slug='dubai-open-house-august-2026' ORDER BY r.created_at DESC`;const headers=['full_name','phone','email','purpose','budget','property_type','preferred_area','purchase_timeline','preferred_event_date','status','assigned_to','lead_score','temperature','source','utm_source','utm_medium','utm_campaign','created_at'];res.statusCode=200;res.setHeader('Content-Type','text/csv; charset=utf-8');res.setHeader('Content-Disposition','attachment; filename="finding-stories-open-house-rsvps.csv"');res.end('\uFEFF'+headers.join(',')+'\n'+rows.map(r=>headers.map(h=>csv(r[h])).join(',')).join('\n'));}
