import { database, ensureSchema } from '../_lib/db.js';
import { canonicalUrl, eligiblePage, pageSchema, safeProperty } from '../_lib/acquisition.js';
import { method } from '../_lib/http.js';

const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const money=value=>new Intl.NumberFormat('en-AE',{style:'currency',currency:'AED',maximumFractionDigits:0}).format(value);
function render(page,items,origin){const canonical=canonicalUrl(page.path,origin);const safe=items.map(safeProperty).filter(Boolean);const cards=safe.map(x=>`<article><h2>${esc(x.project)}</h2><dl><div><dt>Developer</dt><dd>${esc(x.developer)}</dd></div><div><dt>Area</dt><dd>${esc(x.area)}</dd></div><div><dt>Unit type</dt><dd>${esc(`${x.bedrooms} ${x.property_type}`)}</dd></div><div><dt>Status</dt><dd>${esc(x.status)}</dd></div><div><dt>Verified source</dt><dd>${esc(x.inventory_provenance)}</dd></div><div><dt>Last verified</dt><dd>${esc(x.last_verified_at)}</dd></div>${x.minimum_price?`<div><dt>Verified price from</dt><dd>${esc(money(x.minimum_price))}</dd></div>`:''}${x.handover?`<div><dt>Handover</dt><dd>${esc(x.handover)}</dd></div>`:''}${x.payment_plan_summary?`<div><dt>Payment plan</dt><dd>${esc(x.payment_plan_summary)}</dd></div>`:''}</dl></article>`).join('');const context=page.area?`Explore verified property records in ${esc(page.area)}.`:`Explore verified records for ${esc(page.project)} by ${esc(page.developer)}.`;return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(page.intent)} | Finding Stories</title><meta name="description" content="Verified ${esc(page.intent)} options with a Finding Stories advisor."><link rel="canonical" href="${esc(canonical)}"><meta property="og:title" content="${esc(page.intent)}"><meta property="og:description" content="Review verified options and request human advisor guidance."><meta property="og:url" content="${esc(canonical)}"><meta property="og:type" content="website"><script type="application/ld+json">${JSON.stringify(pageSchema(page,items,origin)).replaceAll('<','\\u003c')}</script><link rel="stylesheet" href="/public/acquisition.css"></head><body><header><a href="/">Finding Stories</a><a class="cta" data-signal="whatsapp_click" href="https://wa.me/971505256750" rel="noopener">WhatsApp an advisor</a></header><main><nav aria-label="Breadcrumb"><a href="/">Home</a> / <span>${esc(page.intent)}</span></nav><p class="eyebrow">VERIFIED PROPERTY DISCOVERY</p><h1>${esc(page.intent)}</h1><p class="intro">${context} Availability and investment outcomes must be confirmed with a human advisor; no return is guaranteed.</p><section aria-label="Verified options" class="cards">${cards}</section><section class="enquire"><div><h2>Discuss your property requirement</h2><p>Submit your preferences for advisor review. No customer communication is sent without human approval.</p></div><form id="lead-form"><input name="name" placeholder="Name" required minlength="2"><input name="phone" placeholder="Phone" required minlength="7"><input name="email" type="email" placeholder="Email (optional)"><input name="budget" placeholder="Budget"><input name="bedrooms" placeholder="Bedrooms"><textarea name="additional_requirements" placeholder="What are you looking for?"></textarea><label><input name="consent" type="checkbox" required> I agree to be contacted about this enquiry.</label><button>Request advisor review</button><p id="form-status" role="status"></p></form></section></main><script>window.__ACQUISITION__=${JSON.stringify({page_url:page.path,page_type:page.category.toLowerCase(),area:page.area||'',project:page.project||'',developer:page.developer||''}).replaceAll('<','\\u003c')}</script><script src="/public/acquisition.js" type="module"></script></body></html>`;}
export default async function handler(req,res){if(!method(req,res,['GET']))return;try{await ensureSchema();const sql=database();const inventory=await sql`SELECT * FROM property_inventory WHERE status='active' AND is_test=FALSE AND data_quality='verified'`;const path=`/${String(req.query?.path||'').replace(/^\/+|\/+$/g,'')}`;const page=eligiblePage(path,inventory);if(!page){res.statusCode=404;res.setHeader('X-Robots-Tag','noindex, nofollow');return res.end('Not found');}const selected=inventory.filter(x=>page.inventory_ids.includes(x.id));res.statusCode=200;res.setHeader('Content-Type','text/html; charset=utf-8');res.setHeader('Cache-Control','public, s-maxage=900, stale-while-revalidate=86400');return res.end(render(page,selected,process.env.PUBLIC_SITE_URL||'https://finding-stories.com'));}catch{res.statusCode=503;res.setHeader('X-Robots-Tag','noindex, nofollow');return res.end('Property data is temporarily unavailable.');}}
