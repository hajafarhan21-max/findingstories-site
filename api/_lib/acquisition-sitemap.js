import { database, ensureSchema } from '../_lib/db.js';
import { canonicalUrl, discoverOpportunities } from '../_lib/acquisition.js';
import { method } from '../_lib/http.js';
export default async function handler(req,res){if(!method(req,res,['GET']))return;try{await ensureSchema();const sql=database();const inventory=await sql`SELECT * FROM property_inventory WHERE status='active' AND is_test=FALSE AND data_quality='verified'`;const origin=process.env.PUBLIC_SITE_URL||'https://www.finding-stories.com';const urls=['/',...discoverOpportunities(inventory).map(x=>x.path)];res.setHeader('Content-Type','application/xml; charset=utf-8');res.setHeader('Cache-Control','public, s-maxage=900');res.end(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${urls.map(x=>`<url><loc>${canonicalUrl(x,origin)}</loc></url>`).join('')}</urlset>`);}catch{res.statusCode=503;res.setHeader('X-Robots-Tag','noindex');res.end('');}}
